diff_match_patch
editdistance
hydra-core
joblib
librosa>=0.10.0 # specify >=0.10.0 so that librosa.get_duration(path=...) will work
numpy
omegaconf
pandas
regex
sox
tqdm
wget
jiwer
webvtt-py # for youtube (wtt procesing in general) 
pysrt # for youtube only
nemo_text_processing # for text normalization
# for some processers, additionally https://github.com/NVIDIA/NeMo is required
